os:
  - linux

language: rust

rust:
  - stable
  - nightly

addons:
  postgresql: "9.5"

services:
  - postgresql

before_script:
  - rustup component add clippy
  - cd crates/rustorm
  - ./execute-data-import.sh
  - cd -


  
script:
  - cargo test --all
  - cargo clippy
